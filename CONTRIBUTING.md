# Contributing

Run `pnpm check` before opening a pull request.

The preset may describe independent plugins, but it must not copy their code,
fetch executable modules, or gain authoring capabilities of its own.
